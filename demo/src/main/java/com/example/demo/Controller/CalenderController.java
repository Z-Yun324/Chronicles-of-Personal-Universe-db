package com.example.demo.Controller;
/*
import com.example.demo.pojo.CalenderList;
import org.springframework.web.bind.annotation.*;

import java.lang.reflect.Member;

@RestController
public class CalenderController {
    @PostMapping("/addcalenderlist")
    public String create(@RequestBody CalenderList calenderList) {
        new CalenderList(calenderList.getC_id(),calenderList.getC_color(),calenderList.getUser_id(),calenderList.getEvent_date(),calenderList.getStart_time(),calenderList.getEnd_time(),calenderList.getThings_description(),calenderList.getReminder_time(),calenderList.getRemark());
        return "已新增行程";
    }
    @GetMapping("/searchcalenderlist/{cid}")
    public String read(@PathVariable String cid) {
        return "執行資料庫的 Read 操作";
    }
    @DeleteMapping("/deletecalenderlist/{cid}")
    public String delete(@PathVariable String cid) {
        return "執行資料庫的 Delete 操作";
    }
    @PutMapping("/updatecalenderlist/{cid}")
    public String update(@PathVariable String cid,
                         @RequestBody Member user) {
        return "執行資料庫的 Update 操作";
    }
}*/
